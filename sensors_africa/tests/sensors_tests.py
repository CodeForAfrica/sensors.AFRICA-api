import pytest
import json

# def test_api_can_get_sensor_list(db, client, location):
#   response = client.get('/v1/sensor', follow=True, format='json')

#   assert response.status_code == 200
#   sensors = json.loads(response.content)
#   assert sensors['count'] == 1